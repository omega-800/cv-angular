import { SkillsSortPipe } from './skills-sort.pipe';

describe('SkillsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
