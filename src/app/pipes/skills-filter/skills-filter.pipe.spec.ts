import { SkillsFilterPipe } from './skills-filter.pipe';

describe('SkillsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
